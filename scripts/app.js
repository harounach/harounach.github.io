$(document).ready(function () {
  /* MVC model */
  const model = {
    /* Initialize Model */
    init: function () {},
  };

  /* MVC controller */
  const controller = {
    /* Initialize Controller */
    init: function () {},
  };

  /* MVC View */
  const view = {
    init: function () {
      // Get references
      this.menuBtn = $(".menu-btn");
    },
  };
});
